<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
    </head>
    <body>
        {% for key, message in app.session.getFlashes() %}
        <div class="{{ key }}">
            {{ message|trans({}, 'merkNotificationBundle') }}
        </div>
        {% endfor %}

        <div>
            {% block merk_notification_content '' %}
        </div>
    </body>
</html>